import { defineConfig } from 'vite';

export default defineConfig({
    // Use relative base path so it works in any subdirectory (like GitHub Pages)
    base: ' https://daffadaminatila.github.io/Sistem-Partikel-3D-Interaktif-Berbasis-Gerakan-Tangan./'
});
